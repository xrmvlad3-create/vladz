# Postgres connection (Neon, Supabase, Vercel Postgres)
# Example (Neon): postgresql://user:password@host/dbname?sslmode=require
DATABASE_URL=""

# Optional: separate connection string for migrations
# DIRECT_URL=""

# Protect POST /api/seed
SEED_SECRET="set-a-random-secret"